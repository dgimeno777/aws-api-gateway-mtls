version: '3.9'

services:
  web:
    image: ${WEB_ECR_REPO_URI}:${USER}
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
  web_prod:
    image: ${WEB_PROD_ECR_REPO_URI}:${USER}
    build:
      context: .
      dockerfile: Dockerfile
      target: prod
